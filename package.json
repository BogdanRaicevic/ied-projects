{
  "name": "ied-monorepo",
  "private": true,
  "version": "2.0.0",
  "description": "Monorepo for IED projects",
  "workspaces": [
    "ied-fe",
    "ied-be",
    "ied-shared",
    "mongo-migrator"
  ],
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "lint": "pnpm -r run lint:all",
    "biome:format": "pnpm -r run biome:format",
    "biome:check": "pnpm -r run biome:check",
    "knip": "knip"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.3",
    "@types/node": "^22.18.1",
    "knip": "^5.63.1",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=22.19.0"
  }
}
